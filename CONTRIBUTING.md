# Contribuindo

1. Instale o Node.js 20 ou superior.
2. Execute `npm install`.
3. Use `npm start` durante o desenvolvimento.
4. Antes de enviar alterações, execute `npm run build`.

Mantenha o editor focado em modelos VRM, animações humanoides e compatibilidade com VRMA 1.0.
